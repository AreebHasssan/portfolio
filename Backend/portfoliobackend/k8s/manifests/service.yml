apiVersion: v1
kind: Service
metadata:
  name: portfolio-backend-service
spec:
  type: NodePort
  selector:
    app: portfolio-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30567