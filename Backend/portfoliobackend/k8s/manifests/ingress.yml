apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portfolio-backend-ingress
  annotations:
    # Essential for backends/APIs to allow cross-origin requests:
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "PUT, GET, POST, OPTIONS, DELETE"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://portfolio-fawn-pi-62.vercel.app"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: api.portfolio.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: portfolio-service
            port:
              number: 5000




#then create ingress controller
